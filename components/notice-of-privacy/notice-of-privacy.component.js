$(document).ready(() => {

	//Initialized the initial components
    async function ngOnInit(){
        system.objects.screenSize(80, 'cover-one');
        system.objects.screenSize(80, 'mobile-cover-one');
    }
    ngOnInit();

});